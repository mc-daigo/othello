"use client";
import styles from "./Footer.module.css"

export const Footer = () => {
  return (
    <footer className={styles.footer}>
      <p className={styles.credit}>React + TypeScript + Next.js Sample</p>
    </footer>
  );
}
